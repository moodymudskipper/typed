# Use the 'typed' package

This sets up your package so you can use 'typed', by editing the
DESCRIPTION file and editing or creating 'R/your.pkg-package.R'

## Usage

``` r
use_typed()
```

## Value

Returns `NULL` invisibly, called for side effects
